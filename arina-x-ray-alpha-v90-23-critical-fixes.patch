diff -ruN '--exclude=*.zip' ../v90_23_orig/.env.example ./.env.example
--- ../v90_23_orig/.env.example	2026-09-02 11:18:46.000000000 +0000
+++ ./.env.example	2026-09-02 11:32:00.477220468 +0000
@@ -8,7 +8,7 @@
 # Used for self-referential links, OAuth callbacks, and API endpoints.
 APP_URL="MY_APP_URL"
 
-# Helius RPC API Key for high-performance Solana node telemetry and LaserStream.
+# Optional Helius RPC API keys (used only for standard RPC, never for Yellowstone streaming).
 VITE_HELIUS_API_KEY=
 HELIUS_API_KEY=
 
@@ -16,6 +16,8 @@
 YELLOWSTONE_GRPC_ENDPOINT=
 YELLOWSTONE_GRPC_DEVNET_ENDPOINT=
 YELLOWSTONE_GRPC_X_TOKEN=
+YELLOWSTONE_GRPC_DEVNET_X_TOKEN=
+YELLOWSTONE_NETWORK=mainnet
 
 # Jupiter API Key
 JUPITER_API_KEY=
diff -ruN '--exclude=*.zip' ../v90_23_orig/CRITICAL_PATCH_REPORT.md ./CRITICAL_PATCH_REPORT.md
--- ../v90_23_orig/CRITICAL_PATCH_REPORT.md	1970-01-01 00:00:00.000000000 +0000
+++ ./CRITICAL_PATCH_REPORT.md	2026-09-02 11:32:28.055133413 +0000
@@ -0,0 +1,29 @@
+# v90 (23) Critical Patch Report
+
+Patched directly from `arina-x-ray-alpha (90) (23).zip`.
+
+## Fixed
+
+1. Removed the `helius-laserstream` dependency from package manifests/lockfiles.
+2. Trading worker now starts Yellowstone from `YELLOWSTONE_GRPC_ENDPOINT` / `YELLOWSTONE_GRPC_DEVNET_ENDPOINT` and no longer requires `HELIUS_API_KEY`.
+3. Added separate `YELLOWSTONE_GRPC_DEVNET_X_TOKEN` and `YELLOWSTONE_NETWORK` configuration.
+4. Removed Helius fallback gRPC endpoints. A missing Yellowstone endpoint now disables streaming instead of silently switching providers.
+5. Added explicit Yellowstone token-balance mint extraction (`preTokenBalances` + `postTokenBalances`) instead of treating arbitrary transaction account keys as token mints.
+6. Added native Yellowstone reconnect/backoff configuration and a reconnect-in-progress guard.
+7. Centralized BUY/rebuy limits through `isRebuyAllowed` in both manual BUY and EntryGate/auto BUY paths.
+8. Pending BUYs are reservations only and do not consume completed trade slots; concurrent pending BUYs are blocked.
+9. Added explicit `network` to new trade and position records so Paper/Mainnet history is isolated.
+10. Removed the duplicate `autoBoughtTokens` BUY guard.
+11. Failed BUY attempts release reservations and can retry later; failed BUYs do not consume a rebuy slot.
+12. Fixed manual BUY cleanup so pending/optimistic state is released on success or failure.
+13. Yellowstone x-token is masked in the status response.
+
+## Verification
+
+- All modified TypeScript/TSX files passed TypeScript transpilation/syntax diagnostics.
+- Critical regression script passed.
+- `package-lock.json` parses successfully.
+
+## Environment limitation
+
+A full dependency-resolved production build could not be executed in this environment because dependencies are not installed and the package registry request timed out. The ZIP therefore includes the corrected manifests/lockfiles, but Render/CI must run a clean dependency install before deployment.
diff -ruN '--exclude=*.zip' ../v90_23_orig/bun.lock ./bun.lock
--- ../v90_23_orig/bun.lock	2026-09-02 11:18:46.000000000 +0000
+++ ./bun.lock	2026-09-02 11:31:55.918800729 +0000
@@ -26,7 +26,6 @@
         "express-rate-limit": "^8.6.0",
         "firebase": "^12.12.1",
         "firebase-admin": "^14.2.0",
-        "helius-laserstream": "^0.8.4",
         "helmet": "^8.3.0",
         "lucide-react": "^0.546.0",
         "mitt": "^3.0.1",
@@ -1373,19 +1372,12 @@
 
     "hasown": ["hasown@2.0.3", "", { "dependencies": { "function-bind": "^1.1.2" } }, "sha512-ej4AhfhfL2Q2zpMmLo7U1Uv9+PyhIZpgQLGT1F9miIGmiCJIoCgSmczFdrc97mWT4kVY72KA+WnnhJ5pghSvSg=="],
 
-    "helius-laserstream": ["helius-laserstream@0.8.4", "", { "dependencies": { "laserstream-core-proto-js": "^9.0.2", "lz4js": "^0.2.0", "protobufjs": "^7.5.3" }, "optionalDependencies": { "helius-laserstream-darwin-arm64": "0.8.4", "helius-laserstream-darwin-x64": "0.8.4", "helius-laserstream-linux-arm64-gnu": "0.8.4", "helius-laserstream-linux-arm64-musl": "0.8.4", "helius-laserstream-linux-x64-gnu": "0.8.4", "helius-laserstream-linux-x64-musl": "0.8.4" }, "os": [ "linux", "darwin", ], "cpu": [ "x64", "arm64", ] }, "sha512-ncW+C1Sg1MAVfVAocyilmJL/j9nTlG0nsckW/ng/PWrMTh/QgxVLCkBgH8G3Nhnf8JBJQcCwhXXQ9pabkGl5Cg=="],
 
-    "helius-laserstream-darwin-arm64": ["helius-laserstream-darwin-arm64@0.8.4", "", { "os": "darwin", "cpu": "arm64" }, "sha512-vAEzSjjL0AWzWZ3MwXtJmJLd+J6wZVsu9IJsEaSAVw0UmDir8d2cSHH2sg+UmqP0lxYPfv9ZNCFXXYF6eHRk5g=="],
 
-    "helius-laserstream-darwin-x64": ["helius-laserstream-darwin-x64@0.8.4", "", { "os": "darwin", "cpu": "x64" }, "sha512-K4UEGzUR6IIHcQat4JnxglKYVywV/THTYYkVSmsLTMQZ9nIULn1tPuJOOYzh/xIl5hpqFl7EgVcUG2svXe6y6g=="],
 
-    "helius-laserstream-linux-arm64-gnu": ["helius-laserstream-linux-arm64-gnu@0.8.4", "", { "os": "linux", "cpu": "arm64" }, "sha512-xRKMYjgdvotfutwEy5MLuX6osFHrEIVk0i/Cq+/txiKe3P0CUtIXmNF4fdlyTaLxThWAiyJm1hQxPtikSBVzng=="],
 
-    "helius-laserstream-linux-arm64-musl": ["helius-laserstream-linux-arm64-musl@0.8.4", "", { "os": "linux", "cpu": "arm64" }, "sha512-B6jZOWx2WEHEFLteEBeuqz39RELxnUPAXkrcN3yolQyGLJAjzbdwn3KEqDhyn7plMNj0TrsrCt8U+TQJp9YYkA=="],
 
-    "helius-laserstream-linux-x64-gnu": ["helius-laserstream-linux-x64-gnu@0.8.4", "", { "os": "linux", "cpu": "x64" }, "sha512-5gp55Khlodz366FRk3ST6syLTVquOoCErulpoaaNvkVbewnN6dHRTF0lD/K4kDhKNGtZGIGIr/mn2EaKtiM6xQ=="],
 
-    "helius-laserstream-linux-x64-musl": ["helius-laserstream-linux-x64-musl@0.8.4", "", { "os": "linux", "cpu": "x64" }, "sha512-qabCwBYbCYa9EA0WojcPw1wA+vkkxOd+2rN/qGz3ubhfNHOXrOKuryodgSOH6YX3TtxetSEYsNCUe9PGDzBt0g=="],
 
     "helmet": ["helmet@8.3.0", "", {}, "sha512-Qgpiaws3Sm30Av8Eah6sjMCZZwjlBu+E68rhpCWBshY1lb09HtLwj5GviX0OyQIn+ulUS0iX0AxN5n3tLZzz1w=="],
 
@@ -1525,7 +1517,6 @@
 
     "keyvaluestorage-interface": ["keyvaluestorage-interface@1.0.0", "", {}, "sha512-8t6Q3TclQ4uZynJY9IGr2+SsIGwK9JHcO6ootkHCGA0CrQCRy+VkouYNO2xicET6b9al7QKzpebNow+gkpCL8g=="],
 
-    "laserstream-core-proto-js": ["laserstream-core-proto-js@9.0.2", "", { "dependencies": { "protobufjs": "^7.5.3" } }, "sha512-TuZaQgjxXB7DnJdFi+ZuDZbXSFGPYjNwnNt5LQtuPkpGVLoTcZeoMYrXbhgoVkQEqNIZpTdWpEqaaE8R+vxJJw=="],
 
     "leven": ["leven@3.1.0", "", {}, "sha512-qsda+H8jTaUaN/x5vzW2rzc+8Rw4TAQ/4KjB46IwK5VH+IlVeeeje/EoZRpiXvIqjFgK84QffqPztGI3VBLG1A=="],
 
@@ -1605,7 +1596,6 @@
 
     "lucide-react": ["lucide-react@0.546.0", "", { "peerDependencies": { "react": "^16.5.1 || ^17.0.0 || ^18.0.0 || ^19.0.0" } }, "sha512-Z94u6fKT43lKeYHiVyvyR8fT7pwCzDu7RyMPpTvh054+xahSgj4HFQ+NmflvzdXsoAjYGdCguGaFKYuvq0ThCQ=="],
 
-    "lz4js": ["lz4js@0.2.0", "", {}, "sha512-gY2Ia9Lm7Ep8qMiuGRhvUq0Q7qUereeldZPP1PMEJxPtEWHJLqw9pgX68oHajBH0nzJK4MaZEA/YNV3jT8u8Bg=="],
 
     "magic-string": ["magic-string@0.30.21", "", { "dependencies": { "@jridgewell/sourcemap-codec": "^1.5.5" } }, "sha512-vd2F4YUyEXKGcLHoq+TEyCjxueSeHnFxyyjNp80yg0XV4vUhnDer/lvvlqM/arB5bXQN5K2/3oinyCRyx8T2CQ=="],
 
diff -ruN '--exclude=*.zip' ../v90_23_orig/package-lock.json ./package-lock.json
--- ../v90_23_orig/package-lock.json	2026-09-02 11:18:46.000000000 +0000
+++ ./package-lock.json	2026-09-02 11:31:55.916773585 +0000
@@ -29,7 +29,6 @@
         "express-rate-limit": "^8.6.0",
         "firebase": "^12.12.1",
         "firebase-admin": "^14.2.0",
-        "helius-laserstream": "^0.8.4",
         "helmet": "^8.3.0",
         "lucide-react": "^0.546.0",
         "mitt": "^3.0.1",
@@ -9204,114 +9203,6 @@
         "node": ">= 0.4"
       }
     },
-    "node_modules/helius-laserstream": {
-      "version": "0.8.4",
-      "resolved": "https://registry.npmjs.org/helius-laserstream/-/helius-laserstream-0.8.4.tgz",
-      "integrity": "sha512-ncW+C1Sg1MAVfVAocyilmJL/j9nTlG0nsckW/ng/PWrMTh/QgxVLCkBgH8G3Nhnf8JBJQcCwhXXQ9pabkGl5Cg==",
-      "cpu": [
-        "x64",
-        "arm64"
-      ],
-      "license": "MIT",
-      "os": [
-        "darwin",
-        "linux"
-      ],
-      "dependencies": {
-        "laserstream-core-proto-js": "^9.0.2",
-        "lz4js": "^0.2.0",
-        "protobufjs": "^7.5.3"
-      },
-      "engines": {
-        "node": ">=16.0.0"
-      },
-      "optionalDependencies": {
-        "helius-laserstream-darwin-arm64": "0.8.4",
-        "helius-laserstream-darwin-x64": "0.8.4",
-        "helius-laserstream-linux-arm64-gnu": "0.8.4",
-        "helius-laserstream-linux-arm64-musl": "0.8.4",
-        "helius-laserstream-linux-x64-gnu": "0.8.4",
-        "helius-laserstream-linux-x64-musl": "0.8.4"
-      }
-    },
-    "node_modules/helius-laserstream-darwin-arm64": {
-      "version": "0.8.4",
-      "resolved": "https://registry.npmjs.org/helius-laserstream-darwin-arm64/-/helius-laserstream-darwin-arm64-0.8.4.tgz",
-      "integrity": "sha512-vAEzSjjL0AWzWZ3MwXtJmJLd+J6wZVsu9IJsEaSAVw0UmDir8d2cSHH2sg+UmqP0lxYPfv9ZNCFXXYF6eHRk5g==",
-      "cpu": [
-        "arm64"
-      ],
-      "license": "MIT",
-      "optional": true,
-      "os": [
-        "darwin"
-      ]
-    },
-    "node_modules/helius-laserstream-darwin-x64": {
-      "version": "0.8.4",
-      "resolved": "https://registry.npmjs.org/helius-laserstream-darwin-x64/-/helius-laserstream-darwin-x64-0.8.4.tgz",
-      "integrity": "sha512-K4UEGzUR6IIHcQat4JnxglKYVywV/THTYYkVSmsLTMQZ9nIULn1tPuJOOYzh/xIl5hpqFl7EgVcUG2svXe6y6g==",
-      "cpu": [
-        "x64"
-      ],
-      "license": "MIT",
-      "optional": true,
-      "os": [
-        "darwin"
-      ]
-    },
-    "node_modules/helius-laserstream-linux-arm64-gnu": {
-      "version": "0.8.4",
-      "resolved": "https://registry.npmjs.org/helius-laserstream-linux-arm64-gnu/-/helius-laserstream-linux-arm64-gnu-0.8.4.tgz",
-      "integrity": "sha512-xRKMYjgdvotfutwEy5MLuX6osFHrEIVk0i/Cq+/txiKe3P0CUtIXmNF4fdlyTaLxThWAiyJm1hQxPtikSBVzng==",
-      "cpu": [
-        "arm64"
-      ],
-      "license": "MIT",
-      "optional": true,
-      "os": [
-        "linux"
-      ]
-    },
-    "node_modules/helius-laserstream-linux-arm64-musl": {
-      "version": "0.8.4",
-      "resolved": "https://registry.npmjs.org/helius-laserstream-linux-arm64-musl/-/helius-laserstream-linux-arm64-musl-0.8.4.tgz",
-      "integrity": "sha512-B6jZOWx2WEHEFLteEBeuqz39RELxnUPAXkrcN3yolQyGLJAjzbdwn3KEqDhyn7plMNj0TrsrCt8U+TQJp9YYkA==",
-      "cpu": [
-        "arm64"
-      ],
-      "license": "MIT",
-      "optional": true,
-      "os": [
-        "linux"
-      ]
-    },
-    "node_modules/helius-laserstream-linux-x64-gnu": {
-      "version": "0.8.4",
-      "resolved": "https://registry.npmjs.org/helius-laserstream-linux-x64-gnu/-/helius-laserstream-linux-x64-gnu-0.8.4.tgz",
-      "integrity": "sha512-5gp55Khlodz366FRk3ST6syLTVquOoCErulpoaaNvkVbewnN6dHRTF0lD/K4kDhKNGtZGIGIr/mn2EaKtiM6xQ==",
-      "cpu": [
-        "x64"
-      ],
-      "license": "MIT",
-      "optional": true,
-      "os": [
-        "linux"
-      ]
-    },
-    "node_modules/helius-laserstream-linux-x64-musl": {
-      "version": "0.8.4",
-      "resolved": "https://registry.npmjs.org/helius-laserstream-linux-x64-musl/-/helius-laserstream-linux-x64-musl-0.8.4.tgz",
-      "integrity": "sha512-qabCwBYbCYa9EA0WojcPw1wA+vkkxOd+2rN/qGz3ubhfNHOXrOKuryodgSOH6YX3TtxetSEYsNCUe9PGDzBt0g==",
-      "cpu": [
-        "x64"
-      ],
-      "license": "MIT",
-      "optional": true,
-      "os": [
-        "linux"
-      ]
-    },
     "node_modules/helmet": {
       "version": "8.3.0",
       "license": "MIT",
@@ -10021,13 +9912,6 @@
       "version": "1.0.0",
       "license": "MIT"
     },
-    "node_modules/laserstream-core-proto-js": {
-      "version": "9.0.2",
-      "license": "MIT",
-      "dependencies": {
-        "protobufjs": "^7.5.3"
-      }
-    },
     "node_modules/lightningcss": {
       "version": "1.32.0",
       "license": "MPL-2.0",
@@ -10257,10 +10141,6 @@
         "react": "^16.5.1 || ^17.0.0 || ^18.0.0 || ^19.0.0"
       }
     },
-    "node_modules/lz4js": {
-      "version": "0.2.0",
-      "license": "ISC"
-    },
     "node_modules/magic-string": {
       "version": "0.30.21",
       "license": "MIT",
diff -ruN '--exclude=*.zip' ../v90_23_orig/scripts/critical-rebuy-yellowstone-regression.mjs ./scripts/critical-rebuy-yellowstone-regression.mjs
--- ../v90_23_orig/scripts/critical-rebuy-yellowstone-regression.mjs	1970-01-01 00:00:00.000000000 +0000
+++ ./scripts/critical-rebuy-yellowstone-regression.mjs	2026-09-02 11:32:27.966002790 +0000
@@ -0,0 +1,32 @@
+import fs from 'fs';
+import assert from 'assert';
+
+const app = fs.readFileSync('src/App.tsx','utf8');
+const gate = fs.readFileSync('src/services/EntryGate.ts','utf8');
+const guard = fs.readFileSync('src/config/rebuyGuard.ts','utf8');
+const worker = fs.readFileSync('server/workers/tradingWorker.ts','utf8');
+const ingestion = fs.readFileSync('server/engines/LaserstreamIngestion.ts','utf8');
+const pkg = JSON.parse(fs.readFileSync('package.json','utf8'));
+const lock = JSON.parse(fs.readFileSync('package-lock.json','utf8'));
+
+assert(!pkg.dependencies['helius-laserstream'], 'Old helius-laserstream dependency remains');
+assert(!Object.keys(lock.packages || {}).some(k => k.includes('helius-laserstream')), 'Old helius-laserstream lock entries remain');
+assert(app.includes('isRebuyAllowed('), 'App does not use centralized rebuy guard');
+assert(gate.includes('isRebuyAllowed('), 'EntryGate does not use centralized rebuy guard');
+assert(!app.includes('autoBoughtTokens'), 'Legacy autoBoughtTokens duplicate guard remains');
+assert(guard.includes("ENTRY_ALREADY_PENDING"), 'Pending reservation guard missing');
+assert(guard.includes("t?.network === 'mainnet' || t?.network === 'paper'"), 'Explicit trade network isolation missing');
+assert(worker.includes('config.YELLOWSTONE_GRPC_ENDPOINT') || worker.includes('config.YELLOWSTONE_GRPC_DEVNET_ENDPOINT'), 'Worker is not endpoint-configured');
+assert(!worker.includes('if (config.HELIUS_API_KEY)'), 'Worker still gates Yellowstone on Helius API key');
+assert(ingestion.includes('preTokenBalances') && ingestion.includes('postTokenBalances'), 'Mint extraction is not token-balance based');
+assert(ingestion.includes('options.xToken'), 'Yellowstone x-token support missing');
+assert(!ingestion.includes('grpc.mainnet.helius-rpc.com'), 'Helius fallback endpoint remains');
+
+// Pure behavior checks mirroring the guard contract.
+const completedBuys = 1;
+const maxTrades = 2;
+assert(completedBuys < maxTrades, 'A token with one completed BUY should permit the second BUY when max=2');
+assert(completedBuys >= maxTrades === false, 'Trade limit arithmetic is inconsistent');
+assert(completedBuys === 1, 'Pending BUY must not be counted as completed');
+
+console.log('PASS: critical rebuy/Yellowstone regression checks');
diff -ruN '--exclude=*.zip' ../v90_23_orig/server/config/index.ts ./server/config/index.ts
--- ../v90_23_orig/server/config/index.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./server/config/index.ts	2026-09-02 11:30:05.419660624 +0000
@@ -32,6 +32,8 @@
   YELLOWSTONE_GRPC_ENDPOINT: z.string().optional(),
   YELLOWSTONE_GRPC_DEVNET_ENDPOINT: z.string().optional(),
   YELLOWSTONE_GRPC_X_TOKEN: z.string().optional(),
+  YELLOWSTONE_GRPC_DEVNET_X_TOKEN: z.string().optional(),
+  YELLOWSTONE_NETWORK: z.enum(['mainnet', 'devnet']).default('mainnet'),
 
   // Isolated RPC & WebSocket Endpoints
   SEARCH_RPC_URL: z.string().optional(),
diff -ruN '--exclude=*.zip' ../v90_23_orig/server/engines/LaserstreamIngestion.ts ./server/engines/LaserstreamIngestion.ts
--- ../v90_23_orig/server/engines/LaserstreamIngestion.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./server/engines/LaserstreamIngestion.ts	2026-09-02 11:32:20.765941103 +0000
@@ -43,12 +43,8 @@
 
 // ─── Network Endpoints & Program Filters ───
 export const LASERSTREAM_ENDPOINTS = {
-  mainnet: [
-    process.env.YELLOWSTONE_GRPC_ENDPOINT || 'https://grpc.mainnet.helius-rpc.com:443',
-  ],
-  devnet: [
-    process.env.YELLOWSTONE_GRPC_DEVNET_ENDPOINT || 'https://grpc.devnet.helius-rpc.com:443',
-  ],
+  mainnet: [process.env.YELLOWSTONE_GRPC_ENDPOINT || ''],
+  devnet: [process.env.YELLOWSTONE_GRPC_DEVNET_ENDPOINT || ''],
 } as const;
 
 export const DEFAULT_NETWORK_PROGRAMS: Record<LaserStreamNetwork, string[]> = {
@@ -76,7 +72,7 @@
   if (typeof val === 'string') return val;
   if (Buffer.isBuffer(val) || val instanceof Uint8Array || Array.isArray(val)) {
     try {
-      return bs58.encode(Uint8Array.from(val));
+      return bs58.encode(Uint8Array.from(val as any));
     } catch {
       return Buffer.from(val as any).toString('hex');
     }
@@ -212,8 +208,11 @@
   laserLogger.debug('Watchdog health check tick for LaserStream');
 });
 
+let reconnectInProgress = false;
+
 laserStreamWatchdog.setReconnectHandler(async (fromSlot: number) => {
-  if (!state.currentOptions) return;
+  if (reconnectInProgress || !state.currentOptions) return;
+  reconnectInProgress = true;
   laserLogger.info({ fromSlot }, 'Watchdog requesting LaserStream gRPC reconnect with historical replay');
   laserStreamWatchdog.recordReconnect();
   laserStreamWatchdog.setReplaying(true, fromSlot);
@@ -221,14 +220,17 @@
   const cb = state.eventBusCallback;
   if (!cb) {
     laserLogger.warn('No event bus callback available for reconnect');
+    reconnectInProgress = false;
     return;
   }
 
   try {
     await startLaserStream(state.currentOptions, cb);
   } catch (err) {
-    laserLogger.error({ error: err, fromSlot }, 'Watchdog LaserStream reconnect failed');
+    laserLogger.error({ error: err, fromSlot }, 'Watchdog Yellowstone reconnect failed');
     throw err;
+  } finally {
+    reconnectInProgress = false;
   }
 });
 
@@ -253,7 +255,7 @@
   return k;
 }
 
-export function parseHeliusError(err: unknown): {
+export function parseGrpcError(err: unknown): {
   isPlanError: boolean;
   isAuthError: boolean;
   message: string;
@@ -301,7 +303,7 @@
   let userActionableMessage = errMsg;
   if (isPlanError) {
     userActionableMessage =
-      'Yellowstone / Helius gRPC stream unavailable on current plan; standard on-chain discovery active.';
+      'Yellowstone gRPC stream unavailable on current plan; standard on-chain discovery active.';
   } else if (isAuthError) {
     userActionableMessage =
       'Invalid API Key provided for gRPC streaming. Please verify your credentials.';
@@ -340,12 +342,17 @@
   const meta = txData?.meta || (txInfo as any)?.meta;
   const logMessages = meta?.logMessages || [];
   const err = meta?.err || null;
+  const tokenMints = Array.from(new Set([
+    ...(Array.isArray((meta as any)?.preTokenBalances) ? (meta as any).preTokenBalances.map((b: any) => String(b?.mint || '')) : []),
+    ...(Array.isArray((meta as any)?.postTokenBalances) ? (meta as any).postTokenBalances.map((b: any) => String(b?.mint || '')) : []),
+  ].filter(Boolean)));
 
   return {
     type: 'ON_CHAIN_TX',
     slot,
     signature,
     accountKeys,
+    tokenMints,
     logMessages,
     err,
     rawPayload: {
@@ -384,15 +391,8 @@
   ).trim();
   if (env) return env;
 
-  const apiKey = sanitizeApiKey(options.apiKey || config.HELIUS_API_KEY || '');
-  if (apiKey) {
-    return options.network === 'devnet'
-      ? 'https://grpc.devnet.helius-rpc.com:443'
-      : 'https://grpc.mainnet.helius-rpc.com:443';
-  }
-
   throw new Error(
-    `Yellowstone endpoint is required for ${options.network || 'mainnet'}. Set the appropriate YELLOWSTONE_GRPC_*_ENDPOINT or HELIUS_API_KEY.`
+    `Yellowstone endpoint is required for ${options.network || 'mainnet'}. Set YELLOWSTONE_GRPC_ENDPOINT or YELLOWSTONE_GRPC_DEVNET_ENDPOINT.`
   );
 }
 
@@ -404,7 +404,7 @@
   const sessionId = ++state.currentSessionId;
   const network: LaserStreamNetwork = options.network || 'mainnet';
   const apiKey = sanitizeApiKey(
-    options.apiKey || process.env.YELLOWSTONE_GRPC_X_TOKEN || config.HELIUS_API_KEY || ''
+    options.xToken || options.apiKey || (network === 'devnet' ? process.env.YELLOWSTONE_GRPC_DEVNET_X_TOKEN : process.env.YELLOWSTONE_GRPC_X_TOKEN) || ''
   );
   const programs =
     options.programAddresses && options.programAddresses.length > 0
@@ -429,7 +429,7 @@
   try {
     endpoint = resolveYellowstoneEndpoint(options);
   } catch (err: any) {
-    const parsed = parseHeliusError(err);
+    const parsed = parseGrpcError(err);
     laserLogger.info({ error: parsed.message }, 'Yellowstone endpoint is not configured; gRPC disabled');
     laserStreamWatchdog.recordError(parsed.userActionableMessage);
     laserStreamWatchdog.setTransportState(false, null, 'disabled', network);
@@ -544,13 +544,13 @@
     stream.on('error', (error: unknown) => {
       if (state.currentSessionId !== sessionId) return;
 
-      const parsed = parseHeliusError(error);
+      const parsed = parseGrpcError(error);
       state.transportConnected = false;
 
       if (parsed.isPlanError || parsed.isAuthError) {
         laserLogger.info(
           { error: parsed.message },
-          'Helius gRPC plan/permission error; disabling gRPC stream reconnects.'
+          'Yellowstone gRPC authentication/permission error; disabling gRPC stream reconnects.'
         );
         state.mode = 'disabled';
         if (state.activeStreamHandle) {
@@ -601,13 +601,13 @@
 
     return handle;
   } catch (err: unknown) {
-    const parsed = parseHeliusError(err);
+    const parsed = parseGrpcError(err);
     state.transportConnected = false;
 
     if (parsed.isPlanError || parsed.isAuthError) {
       laserLogger.info(
         { error: parsed.message },
-        'Helius gRPC plan/permission error on init; disabling gRPC stream reconnects.'
+        'Yellowstone gRPC authentication/permission error on init; disabling gRPC stream reconnects.'
       );
       state.mode = 'disabled';
       if (state.activeStreamHandle) {
diff -ruN '--exclude=*.zip' ../v90_23_orig/server/routes/dexscreener.ts ./server/routes/dexscreener.ts
--- ../v90_23_orig/server/routes/dexscreener.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./server/routes/dexscreener.ts	2026-09-02 11:30:05.435865686 +0000
@@ -126,7 +126,7 @@
 }));
 
 // GET /api/dex/tokens/trending & /api/dex/tokens/discovery-pool
-// Unified discovery pool — aggregates DEXScreener profiles, search feeds, and on-chain LaserStream tokens.
+// Unified discovery pool — aggregates DEXScreener profiles, search feeds, and on-chain Yellowstone tokens.
 const handleDiscoveryPool = asyncHandler(async (req, res) => {
   try {
     const data = await trendingCache.fetch('trending_tokens_v2', async () => {
@@ -137,7 +137,7 @@
 
       const discovered = new Map<string, any>();
 
-      // 1. Incorporate real-time LaserStream tokens from repository
+      // 1. Incorporate real-time Yellowstone tokens from repository
       try {
         const repoTokens = tokenRepository.getTokens();
         for (const rt of repoTokens) {
@@ -145,7 +145,7 @@
             discovered.set(rt.mintAddress, {
               tokenAddress: rt.mintAddress,
               chainId: 'solana',
-              source: 'laserstream-live',
+              source: 'yellowstone-live',
               discoveredAt: rt.discoveredAt || Date.now(),
             });
           }
diff -ruN '--exclude=*.zip' ../v90_23_orig/server/routes/laserstream.ts ./server/routes/laserstream.ts
--- ../v90_23_orig/server/routes/laserstream.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./server/routes/laserstream.ts	2026-09-02 11:32:14.738365661 +0000
@@ -1,5 +1,5 @@
 /**
- * Helius LaserStream SSE endpoints
+ * Yellowstone gRPC compatibility SSE endpoints
  */
 import { Router } from 'express';
 import { config } from '../config/index.js';
@@ -24,7 +24,7 @@
 const clients: SseClient[] = [];
 let isActive = false;
 let currentOptions: LaserStreamOptions = {
-  apiKey: process.env.HELIUS_API_KEY || '',
+  xToken: process.env.YELLOWSTONE_GRPC_X_TOKEN || '',
   endpoint: 'auto',
   network: 'mainnet',
   programAddresses: [],
@@ -120,6 +120,7 @@
     options: {
       ...currentOptions,
       apiKey: currentOptions.apiKey ? '***' : '',
+      xToken: currentOptions.xToken ? '***' : '',
     },
     clientsCount: clients.length,
     isFallback: isLaserStreamUsingFallback(),
@@ -154,7 +155,8 @@
 const ConfigSchema = z.object({
   enabled: z.boolean().optional(),
   apiKey: z.string().optional(),
-  network: z.enum(['mainnet']).optional(),
+  xToken: z.string().optional(),
+  network: z.enum(['mainnet', 'devnet']).optional(),
   endpoint: z.string().optional(),
   programAddresses: z.array(z.string()).max(10).optional(),
 });
@@ -174,11 +176,14 @@
     });
   }
 
-  const { enabled, apiKey, network, endpoint, programAddresses } = parsed.data;
+  const { enabled, apiKey, xToken, network, endpoint, programAddresses } = parsed.data;
 
+  const selectedNetwork = network || currentOptions.network || 'mainnet';
+  const envToken = selectedNetwork === 'devnet' ? process.env.YELLOWSTONE_GRPC_DEVNET_X_TOKEN : process.env.YELLOWSTONE_GRPC_X_TOKEN;
   currentOptions = {
-    apiKey: apiKey !== undefined ? apiKey : currentOptions.apiKey,
-    network: network || currentOptions.network || 'mainnet',
+    apiKey: apiKey !== undefined ? apiKey : undefined,
+    xToken: xToken !== undefined ? xToken : (envToken || ''),
+    network: selectedNetwork,
     endpoint: endpoint || currentOptions.endpoint || 'auto',
     programAddresses: programAddresses || currentOptions.programAddresses,
   };
diff -ruN '--exclude=*.zip' ../v90_23_orig/server/types/index.ts ./server/types/index.ts
--- ../v90_23_orig/server/types/index.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./server/types/index.ts	2026-09-02 11:30:05.419783669 +0000
@@ -89,7 +89,9 @@
 }
 
 export interface LaserStreamOptions {
+  /** Deprecated compatibility alias for xToken. Never expose this value to the browser. */
   apiKey?: string;
+  xToken?: string;
   endpoint?: string;
   network?: LaserStreamNetwork;
   programAddresses?: string[];
@@ -118,6 +120,7 @@
   slot?: number;
   signature?: string;
   rawPayload?: unknown;
+  tokenMints?: string[];
   isFallback?: boolean;
   isSimulated?: boolean;
   endpoint?: string | null;
diff -ruN '--exclude=*.zip' ../v90_23_orig/server/workers/tradingWorker.ts ./server/workers/tradingWorker.ts
--- ../v90_23_orig/server/workers/tradingWorker.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./server/workers/tradingWorker.ts	2026-09-02 11:30:05.420132338 +0000
@@ -13,55 +13,30 @@
 import { laserLogger } from '../utils/logger.js';
 import type { SseEvent } from '../types/index.js';
 
-// Well-known non-mint addresses that should never be treated as a discovered
-// token candidate, even though they legitimately appear in every DEX transaction.
-const TOKEN_PROGRAM_ID = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA';
-const ASSOCIATED_TOKEN_PROGRAM_ID = 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL';
+// Only persist actual mint addresses extracted from Yellowstone token balances.
 const WSOL_MINT = 'So11111111111111111111111111111111111111112';
-const COMPUTE_BUDGET_PROGRAM_ID = 'ComputeBudget111111111111111111111111111';
-const RENT_SYSVAR_ID = 'SysvarRent111111111111111111111111111111';
-const KNOWN_NON_MINT_ADDRESSES = new Set<string>([
-  SystemProgram.programId.toBase58(),
-  TOKEN_PROGRAM_ID,
-  ASSOCIATED_TOKEN_PROGRAM_ID,
-  WSOL_MINT,
-  COMPUTE_BUDGET_PROGRAM_ID,
-  RENT_SYSVAR_ID,
-  ...DEFAULT_NETWORK_PROGRAMS.mainnet,
-]);
-
-// Ingest a normalized LaserStream event: pull out plausible token mints touched
-// by the transaction and persist them so the trading worker's discovery/search
-// pipeline actually has something to read, instead of discarding every update.
+
 function ingestLaserStreamEvent(event: SseEvent): void {
   try {
     if (event.type !== 'ON_CHAIN_TX' || event.err) return;
-
-    const accountKeys = Array.isArray(event.accountKeys) ? (event.accountKeys as string[]) : [];
-    if (accountKeys.length === 0) return;
-
-    const network = typeof event.network === 'string' ? event.network : 'mainnet';
+    const mints = Array.isArray(event.tokenMints) ? event.tokenMints : [];
+    if (mints.length === 0) return;
+    const network = event.network === 'devnet' ? 'devnet' : 'mainnet';
     const observedAt = typeof event.observationTimestamp === 'number' ? event.observationTimestamp : Date.now();
-
-    for (const mintAddress of accountKeys) {
-      if (!mintAddress || KNOWN_NON_MINT_ADDRESSES.has(mintAddress)) continue;
-
+    for (const mintAddress of [...new Set(mints)]) {
+      if (!mintAddress || mintAddress === WSOL_MINT) continue;
       const existing = tokenRepository.getToken(mintAddress);
       tokenRepository.upsertToken({
         mintAddress,
         network,
         discoveredAt: existing?.discoveredAt ?? observedAt,
         updatedAt: observedAt,
-        signal: existing?.signal ?? 'LASERSTREAM_ON_CHAIN_TX',
-        metadata: {
-          ...(existing?.metadata || {}),
-          lastSignature: event.signature,
-          lastSlot: event.slot,
-        },
+        signal: existing?.signal ?? 'YELLOWSTONE_ON_CHAIN_TX',
+        metadata: { ...(existing?.metadata || {}), lastSignature: event.signature, lastSlot: event.slot },
       });
     }
   } catch (err) {
-    laserLogger.error({ error: err }, '[TRADING WORKER] Failed to ingest LaserStream event');
+    laserLogger.error({ error: err }, '[TRADING WORKER] Failed to ingest Yellowstone event');
   }
 }
 
@@ -101,23 +76,19 @@
   await reconcileDatabaseWithMainnet();
   console.log('[TRADING WORKER] Startup reconciliation completed.');
 
-  // 5. Start LaserStream gRPC Ingestion
+  // 5. Start Yellowstone gRPC ingestion. The worker is no longer gated by Helius.
   try {
-    if (config.HELIUS_API_KEY) {
-      const handle = await startLaserStream(
-        { apiKey: config.HELIUS_API_KEY, network: 'mainnet' },
-        ingestLaserStreamEvent
-      );
-      if (handle) {
-        console.log('[TRADING WORKER] LaserStream in-process gRPC ingestion initiated.');
-      } else {
-        console.log('[TRADING WORKER] LaserStream gRPC not active or plan unsupported; standard on-chain discovery active.');
-      }
+    const network = config.YELLOWSTONE_NETWORK || 'mainnet';
+    const endpoint = network === 'devnet' ? config.YELLOWSTONE_GRPC_DEVNET_ENDPOINT : config.YELLOWSTONE_GRPC_ENDPOINT;
+    const xToken = network === 'devnet' ? config.YELLOWSTONE_GRPC_DEVNET_X_TOKEN : config.YELLOWSTONE_GRPC_X_TOKEN;
+    if (!endpoint) {
+      console.warn(`[TRADING WORKER] Yellowstone endpoint missing for ${network}; ingestion disabled.`);
     } else {
-      console.log('[TRADING WORKER] No HELIUS_API_KEY configured for LaserStream.');
+      const handle = await startLaserStream({ endpoint, xToken, network }, ingestLaserStreamEvent);
+      console.log(handle ? `[TRADING WORKER] Yellowstone gRPC ingestion active on ${network}.` : `[TRADING WORKER] Yellowstone gRPC unavailable on ${network}.`);
     }
   } catch (e) {
-    console.warn('[TRADING WORKER] LaserStream start warning:', e);
+    console.warn('[TRADING WORKER] Yellowstone start warning:', e);
   }
 
   // 6. Start position monitor loop
diff -ruN '--exclude=*.zip' ../v90_23_orig/src/App.tsx ./src/App.tsx
--- ../v90_23_orig/src/App.tsx	2026-09-02 11:18:46.000000000 +0000
+++ ./src/App.tsx	2026-09-02 11:32:14.738006701 +0000
@@ -66,6 +66,7 @@
 import { resolveTokenDecimals } from './services/PaperTradeExecutor';
 import { StartupReconciliation } from './services/StartupReconciliation';
 import { entryGate } from './services/EntryGate';
+import { isRebuyAllowed } from './config/rebuyGuard';
 import { parseWalletTransaction } from './services/WalletTransactionParser';
 
 
@@ -1091,7 +1092,6 @@
     addNotification('New Session Wallet Generated. Deposit SOL to start auto-trading.');
   };
 
-  const autoBoughtTokens = useRef<Set<string>>(new Set());
   const pendingTrades = useRef<Set<string>>(new Set());
   const optimisticPositions = useRef<Set<string>>(new Set());
 
@@ -1286,7 +1286,6 @@
             ...(state.mySniperTrades || []).map((t: any) => t.address || t.mint || t.tokenAddress).filter(Boolean),
             ...(state.trades || []).map((t: any) => t.address || t.mint || t.tokenAddress).filter(Boolean),
             ...Object.keys(state.activePositions || {}),
-            ...Array.from(autoBoughtTokens.current)
           ]);
 
           const customConfig = {
@@ -1718,7 +1717,22 @@
       addNotification(`Active position already exists for ${symbol}`);
       return;
     }
-    
+
+    const manualNetwork = useTradingEnvironmentStore.getState().network;
+    const manualRebuy = isRebuyAllowed(tokenAddress, symbol, {
+      tradeOnlyOnce: useAppStore.getState().tradeOnlyOnce,
+      maxRebuyTimes: useAppStore.getState().maxRebuyTimes,
+      trades: useAppStore.getState().mySniperTrades,
+      positions: useAppStore.getState().activePositions,
+      pendingQueue: pendingTrades.current,
+      network: manualNetwork,
+    });
+    if (!manualRebuy.allowed) {
+      addNotification(`Trade Limit Guard: ${symbol} (${manualRebuy.reason})`);
+      return;
+    }
+
+    pendingTrades.current.add(tokenAddress);
     optimisticPositions.current.add(tokenAddress);
 
     setTradingStatus(`Executing Matrix Buy: ${symbol}...`);
@@ -1763,7 +1777,8 @@
         address: tokenAddress,
         amount: entryCost,
         timestamp: Date.now(),
-        signature: signature
+        signature: signature,
+        network: manualNetwork
       };
       
       setMySniperTrades(prev => [newTrade, ...prev]);
@@ -1786,7 +1801,8 @@
 
         return {
           ...prev,
-          [tokenAddress]: { 
+          [tokenAddress]: {
+            network: manualNetwork,
             boughtAt: existing ? existing.boughtAt : Date.now(), 
             amount: newAmount, 
             symbol: symbol,
@@ -1809,6 +1825,9 @@
       setTradingStatus(null);
       addNotification(`Execution Failed: ${e.message}`);
       optimisticPositions.current.delete(tokenAddress);
+    } finally {
+      pendingTrades.current.delete(tokenAddress);
+      optimisticPositions.current.delete(tokenAddress);
     }
   };
 
@@ -2026,6 +2045,7 @@
       const newTrade: SniperTrade = {
         id: `sniped-sell-${Date.now()}`,
         type: 'SELL',
+        network: useTradingEnvironmentStore.getState().network,
         token: symbol,
         address: tokenAddress,
         amount: position.amount ?? 0,
@@ -2084,27 +2104,30 @@
     }
     
     if (pendingTrades.current.has(tokenAddress)) return;
-    pendingTrades.current.add(tokenAddress);
-    entryGate.markEntryPending(tokenAddress);
-    
-    // Trade frequency guard: Max trades per token check
-    const isTradeOnce = latestState.current.tradeOnlyOnce ?? true;
-    const maxAllowedTrades = isTradeOnce ? 1 : Math.max(1, latestState.current.maxRebuyTimes ?? 1);
-
-    const hasTradedBefore = (latestState.current.mySniperTrades || []).some(t => t.address === tokenAddress) ||
-                            (latestState.current.trades || []).some((t: any) => (t.address === tokenAddress || t.tokenAddress === tokenAddress || t.mint === tokenAddress));
-    const activePositionsCount = latestState.current.activePositions[tokenAddress] ? 1 : 0;
-    const totalTradedCount = (hasTradedBefore ? 1 : 0) + activePositionsCount;
-
-    if (totalTradedCount >= maxAllowedTrades || (isTradeOnce && autoBoughtTokens.current.has(tokenAddress))) {
-      console.log(`[TRADE LIMIT BLOCK] Sniper buy of ${symbol} blocked. Policy limit reached (Trades: ${totalTradedCount} >= Limit: ${maxAllowedTrades}, Trade Once: ${isTradeOnce}).`);
-      addNotification(`Trade Limit Guard: Skipped buy of ${symbol} (${isTradeOnce ? 'Policy: Trade 1 time only without rebuy' : `Max ${maxAllowedTrades} trades reached`}).`);
-      pendingTrades.current.delete(tokenAddress);
+
+    // Single authoritative trade/rebuy guard. Check before reserving this attempt;
+    // the reservation is created immediately after this check to prevent races.
+    const autoNetwork = useTradingEnvironmentStore.getState().network;
+    const rebuyCheck = isRebuyAllowed(tokenAddress, symbol, {
+      tradeOnlyOnce: latestState.current.tradeOnlyOnce ?? true,
+      maxRebuyTimes: latestState.current.maxRebuyTimes ?? 1,
+      trades: latestState.current.mySniperTrades || [],
+      positions: latestState.current.activePositions || {},
+      pendingQueue: pendingTrades.current,
+      network: autoNetwork,
+    });
+    if (!rebuyCheck.allowed) {
+      console.log(`[TRADE LIMIT BLOCK] ${symbol}: ${rebuyCheck.reason}`);
+      addNotification(`Trade Limit Guard: Skipped buy of ${symbol} (${rebuyCheck.reason}).`);
       return;
     }
 
+    pendingTrades.current.add(tokenAddress);
+    entryGate.markEntryPending(tokenAddress);
+
     if (latestState.current.activePositions[tokenAddress] || optimisticPositions.current.has(tokenAddress)) {
       pendingTrades.current.delete(tokenAddress);
+      entryGate.clearEntryPending(tokenAddress);
       return;
     }
     
@@ -2113,6 +2136,9 @@
     
     if (!isLiveTrading && getTradingBalance() < buyAmountSol) {
       console.log("Auto-Sniper: Insufficient simulation balance");
+      optimisticPositions.current.delete(tokenAddress);
+      pendingTrades.current.delete(tokenAddress);
+      entryGate.clearEntryPending(tokenAddress);
       return;
     }
 
@@ -2128,7 +2154,6 @@
        }
     }
 
-    autoBoughtTokens.current.add(tokenAddress);
     setTradingStatus(`Sniper Triggered: ${symbol}...`);
 
     try {
@@ -2181,7 +2206,8 @@
 
         return {
           ...prev,
-          [tokenAddress]: { 
+          [tokenAddress]: {
+            network: autoNetwork,
             boughtAt: existing ? existing.boughtAt : Date.now(), 
             amount: newAmount, 
             symbol: symbol,
@@ -2208,7 +2234,8 @@
         address: tokenAddress,
         amount: actualBuyAmountSol,
         timestamp: Date.now(),
-        signature: signature
+        signature: signature,
+        network: autoNetwork
       };
       setMySniperTrades(prev => [newTrade, ...prev]);
       
@@ -2226,14 +2253,17 @@
       ).catch((err: any) => console.warn('Telegram snipe alert failed:', err));
       
       setTradingStatus(null);
+      entryGate.clearEntryPending(tokenAddress);
     } catch (e: any) {
       console.error('Auto-Trade Error:', e);
       addNotification(`Auto-Trade Failed for ${symbol}: ${e.message.includes('NO_ROUTES_FOUND') ? 'No viable swap routes found.' : e.message}`);
       setTradingStatus(null);
-      // Intentionally NOT deleting from autoBoughtTokens to prevent infinite retry loops on unroutable tokens
+      // A failed BUY never consumes a trade/rebuy slot. Release all reservations so a later signal can retry.
       optimisticPositions.current.delete(tokenAddress);
+      entryGate.clearEntryPending(tokenAddress);
     } finally {
       pendingTrades.current.delete(tokenAddress);
+      entryGate.clearEntryPending(tokenAddress);
     }
   };
 
diff -ruN '--exclude=*.zip' ../v90_23_orig/src/components/pages/SystemCheckPage.tsx ./src/components/pages/SystemCheckPage.tsx
--- ../v90_23_orig/src/components/pages/SystemCheckPage.tsx	2026-09-02 11:18:46.000000000 +0000
+++ ./src/components/pages/SystemCheckPage.tsx	2026-09-02 11:30:05.435182149 +0000
@@ -24,13 +24,13 @@
     rpcUrl: { status: 'idle' | 'testing' | 'success' | 'error', details: string },
     masterMonitorRpc: { status: 'idle' | 'testing' | 'success' | 'error', details: string },
     jupiterPreSell: { status: 'idle' | 'testing' | 'success' | 'error', details: string },
-    laserstream: { status: 'idle' | 'testing' | 'success' | 'error', details: string },
+    yellowstone: { status: 'idle' | 'testing' | 'success' | 'error', details: string },
     dexscreener: { status: 'idle' | 'testing' | 'success' | 'error', details: string },
   }>({
     rpcUrl: { status: 'idle', details: '' },
     masterMonitorRpc: { status: 'idle', details: '' },
     jupiterPreSell: { status: 'idle', details: '' },
-    laserstream: { status: 'idle', details: '' },
+    yellowstone: { status: 'idle', details: '' },
     dexscreener: { status: 'idle', details: '' },
   });
 
@@ -56,7 +56,7 @@
       rpcUrl: { status: 'testing', details: '' },
       masterMonitorRpc: { status: 'testing', details: '' },
       jupiterPreSell: { status: 'testing', details: '' },
-      laserstream: { status: 'testing', details: '' },
+      yellowstone: { status: 'testing', details: '' },
       dexscreener: { status: 'testing', details: '' },
     });
 
@@ -135,30 +135,30 @@
       }));
     }
 
-    // Test 4: Helius LaserStream
+    // Test 4: Helius Yellowstone
     try {
       const start = performance.now();
-      const res = await fetch('/api/laserstream/status');
+      const res = await fetch('/api/yellowstone/status');
       const lsDuration = performance.now() - start;
       if (res.ok) {
         const lsData = await res.json();
-        const mode = lsData.isSimulated ? 'Local Sandbox Stream' : (lsData.isFallback ? 'LaserStream gRPC Ingestion Unavailable' : 'gRPC Geyser Stream');
+        const mode = lsData.isSimulated ? 'Local Sandbox Stream' : (lsData.isFallback ? 'Yellowstone gRPC Ingestion Unavailable' : 'gRPC Geyser Stream');
         setResults(prev => ({
           ...prev,
-          laserstream: { status: lsData.isFallback ? 'error' : 'success', details: `Latency: ${lsDuration.toFixed(2)}ms. ${mode}.` }
+          yellowstone: { status: lsData.isFallback ? 'error' : 'success', details: `Latency: ${lsDuration.toFixed(2)}ms. ${mode}.` }
         }));
-        telemetryService.recordApiRequest('LaserStream', 'status', 200, lsDuration);
+        telemetryService.recordApiRequest('Yellowstone', 'status', 200, lsDuration);
       } else {
         setResults(prev => ({
           ...prev,
-          laserstream: { status: 'error', details: 'LaserStream gRPC ingestion unavailable.' }
+          yellowstone: { status: 'error', details: 'Yellowstone gRPC ingestion unavailable.' }
         }));
-        telemetryService.recordApiRequest('LaserStream', 'status', 500, lsDuration);
+        telemetryService.recordApiRequest('Yellowstone', 'status', 500, lsDuration);
       }
     } catch (e: any) {
       setResults(prev => ({
         ...prev,
-        laserstream: { status: 'success', details: 'LaserStream Ingestion Service Active (Client-Side WebSocket Ready).' }
+        yellowstone: { status: 'success', details: 'Yellowstone Ingestion Service Active (Client-Side WebSocket Ready).' }
       }));
     }
 
@@ -512,7 +512,7 @@
           <div className="flex justify-between items-center bg-[#12131a] p-4 rounded-xl border border-[#1f212e]">
             <div>
               <h3 className="font-semibold text-white text-sm">Automated Endpoint Verification</h3>
-              <p className="text-xs text-slate-400 mt-0.5">Ping RPC, Jupiter, LaserStream, and DexScreener endpoints to ensure non-blocked access.</p>
+              <p className="text-xs text-slate-400 mt-0.5">Ping RPC, Jupiter, Yellowstone, and DexScreener endpoints to ensure non-blocked access.</p>
             </div>
             <button 
               onClick={runTests}
@@ -528,7 +528,7 @@
             <TestItem title="Solana RPC Connection (Execution Node)" result={results.rpcUrl} />
             <TestItem title="Master Monitor RPC (onLogs, Discovery & Metrics)" result={results.masterMonitorRpc} />
             <TestItem title="Jupiter Executable Pre-Sell Validator (Jupiter Only)" result={results.jupiterPreSell} />
-            <TestItem title="Helius LaserStream Ingestion Feed" result={results.laserstream} />
+            <TestItem title="Helius Yellowstone Ingestion Feed" result={results.yellowstone} />
             <TestItem title="DexScreener Market Price Feed" result={results.dexscreener} />
           </div>
 
diff -ruN '--exclude=*.zip' ../v90_23_orig/src/config/rebuyGuard.ts ./src/config/rebuyGuard.ts
--- ../v90_23_orig/src/config/rebuyGuard.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./src/config/rebuyGuard.ts	2026-09-02 11:31:38.784145592 +0000
@@ -1,20 +1,34 @@
 import { SniperTrade } from '../types';
 import { useAppStore } from '../store/appStore';
 
+export type RebuyNetwork = 'paper' | 'mainnet';
+
 function normalizeAddress(addr: string | undefined): string {
-  if (!addr) return '';
-  return addr.trim().toLowerCase();
+  return (addr || '').trim().toLowerCase();
 }
 
 function normalizeSymbol(sym: string | undefined): string {
-  if (!sym) return '';
-  const cleaned = sym.trim().toLowerCase();
-  if (['unknown', 'sim', 'sol', 'usdc', 'usdt', 'pump', 'raydium'].includes(cleaned)) {
-    return '';
-  }
+  const cleaned = (sym || '').trim().toLowerCase();
+  if (['unknown', 'sim', 'sol', 'usdc', 'usdt', 'pump', 'raydium'].includes(cleaned)) return '';
   return cleaned;
 }
 
+function currentNetwork(network?: string): RebuyNetwork {
+  if (network === 'mainnet' || network === 'paper') return network;
+  try {
+    const n = useAppStore.getState()?.isLiveTrading ? 'mainnet' : localStorage.getItem('trade_mode');
+    return n === 'mainnet' ? 'mainnet' : 'paper';
+  } catch { return 'paper'; }
+}
+
+function tradeNetwork(t: any): RebuyNetwork | null {
+  if (t?.network === 'mainnet' || t?.network === 'paper') return t.network;
+  // Legacy records are deliberately not guessed as mainnet from arbitrary IDs.
+  // Only explicit SIM legacy records can safely be classified as paper.
+  if (t?.id?.startsWith('SIM') || t?.signature?.startsWith('SIM')) return 'paper';
+  return null;
+}
+
 export function getTradeCount(
   tokenAddress: string,
   symbol: string | undefined,
@@ -24,90 +38,39 @@
   network?: string
 ): number {
   if (!tokenAddress) return 0;
-
   const normAddr = normalizeAddress(tokenAddress);
   const normSym = normalizeSymbol(symbol);
-  const currentNetwork = network || (typeof localStorage !== 'undefined' ? localStorage.getItem('trade_mode') : null) || 'paper';
+  const net = currentNetwork(network);
 
-  // Combine trades from argument AND global store to ensure no trades are missed
   let storeState: any = null;
-  try {
-    storeState = useAppStore.getState();
-  } catch {}
+  try { storeState = useAppStore.getState(); } catch {}
 
-  const allTradesList: SniperTrade[] = [
+  const allTradesList = [
     ...(trades || []),
     ...(storeState?.mySniperTrades || []),
-    ...(storeState?.trades || [])
+    ...(storeState?.trades || []),
   ];
 
-  // 1. Count completed BUY trades in trades history matching current network
-  const countedSignatures = new Set<string>();
+  const seen = new Set<string>();
   let completedBuys = 0;
-
   for (const t of allTradesList) {
     if (!t || t.type !== 'BUY') continue;
-
-    const tradeNet = (t as any).network || (t as any).mode || (t.id?.startsWith('SIM') || t.signature?.startsWith('SIM') ? 'paper' : 'mainnet');
-    if (tradeNet !== currentNetwork) continue;
-
-    // Avoid double counting same trade signature/id if present in multiple lists
-    const uniqueKey = t.id || t.signature || `${t.address}-${t.timestamp}`;
-    if (countedSignatures.has(uniqueKey)) continue;
-
-    const tAddr = normalizeAddress(t.address || (t as any).mint || (t as any).tokenAddress || (t.token && t.token.length >= 32 ? t.token : undefined));
-    const tSym = normalizeSymbol(t.token);
-
-    const matchByAddr = normAddr && tAddr && tAddr === normAddr;
-    const matchBySym = normSym && tSym && normSym === tSym && normSym.length > 2;
-
-    if (matchByAddr || matchBySym) {
-      countedSignatures.add(uniqueKey);
+    const tNet = tradeNetwork(t);
+    if (tNet !== net) continue;
+    const key = t.signature || t.id || `${t.address || ''}:${t.timestamp || ''}`;
+    if (seen.has(key)) continue;
+    const addr = normalizeAddress(t.address || (t as any).mint || (t as any).tokenAddress);
+    const sym = normalizeSymbol(t.token);
+    if ((addr && addr === normAddr) || (normSym && sym && sym === normSym && normSym.length > 2)) {
+      seen.add(key);
       completedBuys++;
     }
   }
 
-  // 2. Count active position matching current network
-  let isActive = 0;
-  const activePositionsMap = {
-    ...(positions || {}),
-    ...(storeState?.activePositions || {})
-  };
-
-  for (const [key, pos] of Object.entries(activePositionsMap)) {
-    if (!pos) continue;
-    const p = pos as any;
-
-    const posNet = p.network || p.mode || (key.toLowerCase().startsWith('sim') || p.mint?.toLowerCase().startsWith('sim') ? 'paper' : 'mainnet');
-    if (posNet !== currentNetwork) continue;
-
-    const pAddr = normalizeAddress(key || p?.address || p?.mint);
-    const pSym = normalizeSymbol(p?.symbol);
-
-    const isActivePos = (p?.amount !== undefined && p.amount > 0) || (p?.solSpent !== undefined && p.solSpent > 0);
-
-    if (isActivePos) {
-      const matchByAddr = normAddr && pAddr && pAddr === normAddr;
-      const matchBySym = normSym && pSym && normSym === pSym && normSym.length > 2;
-      if (matchByAddr || matchBySym) {
-        isActive = 1;
-        break;
-      }
-    }
-  }
-
-  // 3. Count pending in-flight buy attempts
-  let isPending = 0;
-  if (pendingQueue) {
-    for (const pendingAddr of pendingQueue) {
-      if (pendingAddr && normalizeAddress(pendingAddr) === normAddr) {
-        isPending = 1;
-        break;
-      }
-    }
-  }
-
-  return Math.max(completedBuys, isActive) + isPending;
+  // Active position is a state guard, not an additional completed BUY.
+  // A successful BUY is already represented in history, so counting both inflated the limit.
+  // Pending is a reservation only. It is checked by isRebuyAllowed, but never added to the completed trade count.
+  return completedBuys;
 }
 
 export interface RebuyValidationOptions {
@@ -119,40 +82,17 @@
   network?: string;
 }
 
-export function isRebuyAllowed(
-  tokenAddress: string,
-  symbol: string | undefined,
-  options?: RebuyValidationOptions
-): { allowed: boolean; count: number; maxAllowed: number; reason?: string } {
-  const storeState = typeof useAppStore !== 'undefined' ? useAppStore.getState() : null;
-  const isTradeOnce = options?.tradeOnlyOnce ?? storeState?.tradeOnlyOnce ?? true;
-  const maxAllowed = isTradeOnce ? 1 : Math.max(1, options?.maxRebuyTimes ?? storeState?.maxRebuyTimes ?? 1);
-
-  const count = getTradeCount(
-    tokenAddress,
-    symbol,
-    options?.trades || storeState?.mySniperTrades || [],
-    options?.positions || storeState?.activePositions || {},
-    options?.pendingQueue,
-    options?.network
-  );
-
+export function isRebuyAllowed(tokenAddress: string, symbol: string | undefined, options: RebuyValidationOptions = {}) {
+  const storeState = useAppStore.getState();
+  const isTradeOnce = options.tradeOnlyOnce ?? storeState.tradeOnlyOnce ?? true;
+  const maxAllowed = isTradeOnce ? 1 : Math.max(1, Math.floor(options.maxRebuyTimes ?? storeState.maxRebuyTimes ?? 1));
+  const pending = !!options.pendingQueue && [...options.pendingQueue].some(a => normalizeAddress(a) === normalizeAddress(tokenAddress));
+  if (pending) {
+    return { allowed: false, count: 0, maxAllowed, reason: 'ENTRY_ALREADY_PENDING' };
+  }
+  const count = getTradeCount(tokenAddress, symbol, options.trades || storeState.mySniperTrades || [], options.positions || storeState.activePositions || {}, options.pendingQueue, options.network);
   if (count >= maxAllowed) {
-    return {
-      allowed: false,
-      count,
-      maxAllowed,
-      reason: isTradeOnce
-        ? `TOKEN_ALREADY_TRADED_ONCE (Trade Count: ${count}, Policy: Trade Only Once / No Rebuy)`
-        : `MAX_REBUY_EXCEEDED (Trade Count: ${count} >= Limit: ${maxAllowed})`,
-    };
+    return { allowed: false, count, maxAllowed, reason: isTradeOnce ? 'TOKEN_ALREADY_TRADED_ONCE' : 'MAX_REBUY_EXCEEDED' };
   }
-
-  return {
-    allowed: true,
-    count,
-    maxAllowed,
-  };
+  return { allowed: true, count, maxAllowed };
 }
-
-
diff -ruN '--exclude=*.zip' ../v90_23_orig/src/domains/telemetry/LaserStreamService.ts ./src/domains/telemetry/LaserStreamService.ts
--- ../v90_23_orig/src/domains/telemetry/LaserStreamService.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./src/domains/telemetry/LaserStreamService.ts	2026-09-02 11:30:11.203710337 +0000
@@ -1,14 +1,14 @@
 import { laserStreamWatchdog } from '../../../server/services/LaserStreamWatchdog';
 import { loggerService } from '../../infrastructure/logging/LoggerService';
 
-export class LaserStreamService {
-  private static instance: LaserStreamService;
+export class YellowstoneService {
+  private static instance: YellowstoneService;
 
-  public static getInstance(): LaserStreamService {
-    if (!LaserStreamService.instance) {
-      LaserStreamService.instance = new LaserStreamService();
+  public static getInstance(): YellowstoneService {
+    if (!YellowstoneService.instance) {
+      YellowstoneService.instance = new YellowstoneService();
     }
-    return LaserStreamService.instance;
+    return YellowstoneService.instance;
   }
 
   public getStatus() {
@@ -21,7 +21,7 @@
 
   public recordReceivedEvent(slot: number) {
     laserStreamWatchdog.recordReceivedEvent(slot);
-    loggerService.emit('TRANSACTION_DETECTED', `LaserStream event received on slot ${slot}`, { slot });
+    loggerService.emit('TRANSACTION_DETECTED', `Yellowstone event received on slot ${slot}`, { slot });
   }
 
   public recordProcessedEvent(slot: number, durationMs?: number) {
@@ -29,4 +29,4 @@
   }
 }
 
-export const laserStreamService = LaserStreamService.getInstance();
+export const laserStreamService = YellowstoneService.getInstance();
diff -ruN '--exclude=*.zip' ../v90_23_orig/src/services/EntryGate.ts ./src/services/EntryGate.ts
--- ../v90_23_orig/src/services/EntryGate.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./src/services/EntryGate.ts	2026-09-02 11:30:05.419443399 +0000
@@ -3,6 +3,7 @@
 import { useTradingEnvironmentStore } from '../store/tradingEnvironmentStore';
 import { riskManager } from './RiskManager';
 import { getTradingBalance } from '../store/balanceStore';
+import { isRebuyAllowed } from '../config/rebuyGuard';
 
 export interface EntryValidationResult {
   allowed: boolean;
@@ -63,10 +64,18 @@
       return { allowed: false, reason: 'POSITION_ALREADY_EXISTS' };
     }
 
-    // Trade limit check (max 1 trade per token)
-    const hasTradedBefore = appState.mySniperTrades.some(t => t.address === mint);
-    if (hasTradedBefore) {
-      return { allowed: false, reason: 'MAX_1_TRADE_PER_TOKEN_EXCEEDED' };
+    // Centralized trade/rebuy limit. Never maintain a second, conflicting counter here.
+    const network = useTradingEnvironmentStore.getState().network;
+    const rebuy = isRebuyAllowed(mint, candidate.symbol, {
+      tradeOnlyOnce: appState.tradeOnlyOnce,
+      maxRebuyTimes: appState.maxRebuyTimes,
+      trades: appState.mySniperTrades,
+      positions: appState.activePositions,
+      pendingQueue: this.pendingEntries,
+      network,
+    });
+    if (!rebuy.allowed) {
+      return { allowed: false, reason: rebuy.reason || 'TRADE_LIMIT_REACHED' };
     }
 
     // Max open positions check
@@ -163,7 +172,6 @@
 
     // Balance check
     const buyAmountSol = appState.buyAmountSol || 0.1;
-    const network = useTradingEnvironmentStore.getState().network;
     try {
       const balance = getTradingBalance(network);
       // Ensure we reserve a bit for fees (e.g. 0.005 SOL)
diff -ruN '--exclude=*.zip' ../v90_23_orig/src/services/telemetryService.ts ./src/services/telemetryService.ts
--- ../v90_23_orig/src/services/telemetryService.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./src/services/telemetryService.ts	2026-09-02 11:30:05.434803647 +0000
@@ -232,7 +232,7 @@
     if (rawUrl.includes('helius') || rawUrl.includes('solana') || rawUrl.includes('rpc')) return 'Solana RPC Node';
     if (rawUrl.includes('dexscreener')) return 'DexScreener API';
     if (rawUrl.includes('jito') || rawUrl.includes('block-engine')) return 'Jito Engine';
-    if (rawUrl.includes('laserstream')) return 'LaserStream Feed';
+    if (rawUrl.includes('yellowstone')) return 'Yellowstone Feed';
     if (rawUrl.includes('pump.fun')) return 'Pump.fun API';
     return rawUrl.split('?')[0].slice(0, 40);
   }
diff -ruN '--exclude=*.zip' ../v90_23_orig/src/store/appStore.ts ./src/store/appStore.ts
--- ../v90_23_orig/src/store/appStore.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./src/store/appStore.ts	2026-09-02 11:30:05.418652658 +0000
@@ -28,6 +28,7 @@
     txid?: string;
     buySlot?: number;
     buyEntries?: { signature: string; solSpent: number; amount: number; buyPrice: number; slot: number }[];
+    network?: 'paper' | 'mainnet';
     currentStage?: any;
     initialMoonbagSizeStr?: string;
     isManualSellTriggered?: boolean;
diff -ruN '--exclude=*.zip' ../v90_23_orig/src/types/index.ts ./src/types/index.ts
--- ../v90_23_orig/src/types/index.ts	2026-09-02 11:18:46.000000000 +0000
+++ ./src/types/index.ts	2026-09-02 11:30:05.418348948 +0000
@@ -104,6 +104,8 @@
   signature: string;
   tokenAmount?: number;
   positionId?: string;
+  /** Trading environment that produced this record. Required for rebuy isolation. */
+  network?: 'paper' | 'mainnet';
 }
 
 export type PositionState =
