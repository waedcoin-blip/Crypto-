// src/services/PaperTradeExecutor.ts
// Fail-Closed Guard for Decimals Validation
// Throws UNRESOLVED_TOKEN_DECIMALS if decimals cannot be resolved
// Throws: Unable to resolve token decimals for mint

export { PaperTradeExecutor } from '../../server/execution/PaperTradeExecutor.js';
